<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

   <mapper namespace="com.spring.board.board.mapper.IBoardMapper">
   	
   	<!-- 
   		# 동적 SQL 쿼리
   	 	태그로 조건문을 사용하여 쿼리의 실행을 동적으로 제어할 수 있는 문법
   	 	중복된 SQL 쿼리를 피할 수 있고, 상황에 따른 능동적 대처가 가능합니다.
   	 	미리 쿼리문을 준비해 놓고, <include refid="이름(id)" /> 를 통해 삽입하여 사용합니다.
	-->
	<sql id="search">
		<if test="condition == 'title'">
	 		WHERE title LIKE CONCAT('%', #{searchWord}, '%')
	 	</if>
	 	<if test="condition == 'writer'">
	 		WHERE writer LIKE CONCAT('%', #{searchWord}, '%')
	 	</if>
	 	<if test="condition == 'content'">
	 		WHERE content LIKE CONCAT('%', #{searchWord}, '%')
	 	</if>
	 	<if test="condition == 'titleContent'">
	 		WHERE title LIKE CONCAT('%', #{searchWord}, '%')
	 		OR content LIKE CONCAT('%', #{searchWord}, '%')
	 	</if>
	</sql>
	 
	<!-- 새글 추가 -->
	<insert id="regist">
		<selectKey resultType="int" keyProperty="groupNo" order="BEFORE">
			SELECT IFNULL(MAX(group_no),0)+1 FROM board
		</selectKey>
		INSERT INTO board
		(title, writer, content, password, group_no, step)
		VALUES(#{title}, #{writer}, #{content}, #{password}, #{groupNo}, #{step})
	</insert>

	<!-- 답글 추가 -->
	<insert id="replyInsert">
		INSERT INTO board
	    (title, writer, content, password, group_no, step, depth)
	    VALUES (#{title}, #{writer}, #{content}, #{password}, #{groupNo}, #{step}, #{depth})
	</insert>
	
	<!-- Step 값 계산 -->
	<select id="getMaxStep" resultType="int">
	    SELECT IFNULL(MAX(step), 0)
	    FROM board
	</select>
	
	<!-- 같은 GroupNo의 개수 계산 -->
	<select id="countByGroupNoAndDepth" parameterType="map" resultType="int">
		SELECT COUNT(*) FROM board WHERE group_no = #{groupNo} AND depth = #{depth}
    </select>

	<!-- 답글의 답글을 달 때 정렬을 위해 step을 update -->
	<update id="updateReplyGroupStep" parameterType="map">
		UPDATE board
		SET step = step + 1
		WHERE step >= #{step}
	</update>
	
	<select id="getList" resultType="board">
		SELECT b.*,
		(SELECT COUNT(*) FROM comment WHERE bno = b.bno) AS commentCnt
		FROM board b
		<include refid="search" />
		ORDER BY group_no DESC, step ASC
		LIMIT #{pageStart}, #{cpp}
	</select>
		
	<select id="getTotal" resultType="int">
		SELECT COUNT(*)
		FROM board
		<include refid="search" />
	</select>
	
	<select id="getContent" resultType="board">
		SELECT * FROM board
		WHERE bno = #{bno}
	</select>
	
	<update id="update">
		UPDATE board
		SET writer=#{writer}, title=#{title}, content=#{content}, update_date = current_timestamp
		WHERE bno = #{bno}
	</update>
	
	<delete id="delete">
		DELETE FROM board
		WHERE bno = #{bno}
	</delete>
	   
   </mapper>